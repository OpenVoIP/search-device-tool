kind: pipeline
type: exec
name: package

platform:
  os: darwin
  arсh: amd64


clone:
  disable: true

workspace:
  path: /Users/tqcenglish/SrcCode/Work/Broadcast/search-device-tool

steps:
  - name: macOS
    commands:
      - echo build macOS Install dmg
      - pwd
      - hover build darwin-dmg --docker
    when:                                
      branch:
      - master

trigger:
  branch:
  - master
  event:
  - push